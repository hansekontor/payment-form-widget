#  use 'devlopment' or 'production'
NODE_ENV = development

# URLs for payment server
URL_SANDBOX = 
URL_PRODUCTION = 

# hosting settings (only required when NODE_ENV != development, PORT unused)
HOSTNAME = 
PORT = 443

# paths should lead to RSA certs (only required if you add scripts to include them)
SSL_CERT = /etc/letsencrypt/live/<yourdomain>/fullchain.pem 
SSL_KEY = /etc/letsencrypt/live/<yourdomain>/privkey.pem
