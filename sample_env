#  for any other value than "development" production settings will be applied 
NODE_ENV = development

# URLs for payment server
URL_SANDBOX = 
URL_PRODUCTION = 

# hosting settings (only required when NODE_ENV != development)
HOSTNAME = 
PORT = 443

# paths should lead to RSA certs
PATH_SSL_CERT = /etc/letsencrypt/live/<yourdomain>/fullchain.pem 
PATH_SSL_KEY = /etc/letsencrypt/live/<yourdomain>/privkey.pem